The / for logging in into the system has the sign in container too far of the side, while the keycloak SSO sign in container is perfectly placed.

The / specifically in the line of Secured - Monitored - Explainable there is an icon which is a check mark, this goes missing when i go into the keycloak SSO.

The MFA/TOTP one-time code should have each square for each digit and separates it via 3 numbers, since the authenticator has 640 597 in the app, it should look like this also. when the user types in a number it goes to the next box for digit input, and if the user backspaces twice to delete the number, once the number is deleted it should go to the previous box.