one key thing i see here, it only consists of heatmap & trends window, instead of a width spanning heatmap, it should be taller not wider, put it on the side. then the filter should be on the top inline with the all synced, but the filter should be be more bigger than the all synced.

There is no container for individual incidents, also thiss would need to have a filter. The filter should contain all columns of the incident database schema, we should also be planning for wildfire incidents.

the side panel for incidents is not functional, its just redirecting back to dashboard.

The export pdf and export excel should first have a quick overview on what is the content. this should be a separate container with filters.

check the FRS and the codebase to confirm what is missing that i didnt tackle.